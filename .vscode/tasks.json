{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build international draughts",
            "type": "shell",
            "command": "g++",
            "args": [
                "*.cpp", "-o", "build", "&&", "./build"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}